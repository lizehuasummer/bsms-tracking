services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - GITLAB_URL=https://gitlab.com
      - GITLAB_TOKEN=${GITLAB_TOKEN}
      - PORT=3001
      - DB_PATH=/app/data/bsms.db
      - CORS_ORIGIN=*
    restart: unless-stopped
